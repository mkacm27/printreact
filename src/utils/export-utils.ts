import { Alert } from 'react-native';

export const exportToPDF = () => {
    Alert.alert("Not Implemented", "PDF export is not yet available in the mobile app.");
};

export const exportToCSV = () => {
    Alert.alert("Not Implemented", "CSV export is not yet available in the mobile app.");
};
