import {AppRegistry} from 'react-native';
import App from './src/App';

// The app name is defined in the 'name' field of your package.json
const appName = 'class-print-flow-native';

AppRegistry.registerComponent(appName, () => App);
